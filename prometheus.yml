global:
  scrape_interval: 5s  # 메트릭 수집 주기 (15초마다 수집)

scrape_configs:
  - job_name: 'spring-boot-app'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['host.docker.internal:8080']  # Spring Boot 애플리케이션이 실행되는 주소